# MMY-iconCut 开发文档（本地）

> 本文档记录 MMY-iconCut 项目的结构、一键打包方案，以及本次跨平台单文件打包过程中踩过的坑与解决办法。
> 面向后续维护者，避免重复踩坑。

---

## 1. 项目说明

MMY-iconCut 是一个基于 **PySide6** 的桌面小工具（图标裁剪/处理）。
目标产物：**Windows 单文件 exe** 与 **macOS 单文件 .app**，均带自定义图标、可拷贝到别的机器直接运行（便携版）。

入口：`main.py`
依赖：`requirements.txt`（PySide6、Pillow 等）

---

## 2. 目录结构

```
MMY-iconCut/
├── main.py                 # 程序入口（PySide6 应用）
├── app/                    # 业务逻辑模块
├── 图标/
│   ├── MMY-AI-Studio-icon.png   # 图标源文件（2048×2048 RGBA，必提交）
│   ├── MMY-AI-Studio-icon.ico   # 由 make_icons.py 生成（Windows exe 图标，生成物）
│   └── MMY-AI-Studio-icon.icns  # 由 make_icons.py 生成（macOS app 图标，生成物）
├── tools/
│   └── make_icons.py       # 图标生成器（PNG → 多尺寸 ICO / ICNS）
├── build.bat               # Windows 一键打包启动器（纯 ASCII + CRLF + 无 BOM）
├── build.ps1               # Windows 实际打包逻辑（PowerShell 5.1，带 UTF-8 BOM）
├── build.sh                # macOS / Linux 一键打包
├── run.bat / run.ps1 / run.sh  # 开发期直接运行（不打包）
├── iconCut.spec             # PyInstaller 生成物（生成物，忽略）
├── dist/                   # 打包输出（生成物，忽略）
├── build/                  # PyInstaller 临时目录（生成物，忽略）
└── README.md               # 对外说明
```

---

## 3. 一键打包用法

### Windows（双击即打包）
```
双击 build.bat
```
`build.bat` 仅做启动（纯 ASCII，避免编码坑），实际逻辑在 `build.ps1`：
探测可用 Python（跳过 Microsoft Store 版/embeddable 精简版）→ 安装 PyInstaller →
用 `tools/make_icons.py` 生成 `.ico` → `PyInstaller --onefile --windowed --icon ... --add-data "图标;图标" main.py`。
产物：`dist\iconCut.exe`（单文件，可拷到任意 Windows 直接运行）。

### macOS / Linux
```bash
chmod +x build.sh
./build.sh
```
按 `uname` 选图标：macOS 用 `.icns`，产物 `dist/iconCut.app`（可整包拷贝）；Linux 用 `.ico`。

---

## 4. 踩坑经验（重点）

### 4.1 PowerShell 5.1 的 `Join-Path` 只接受「一个」子路径
**现象**：`Join-Path $a "tools" "make_icons.py"` 报错
`找不到接受实际参数“make_icons.py”的位置形式参数`（PositionalParameterNotFound）。
**根因**：`Join-Path` 在 Windows PowerShell 5.1 里第二参数之后不再接受位置参数（PowerShell 7+ 才支持多段）。
**解决**：改用 `[System.IO.Path]::Combine($a, "b", "c")`，它原生支持任意多段路径拼接。
**教训**：写跨版本 Windows 脚本时，凡涉及多段路径拼接一律用 `Path.Combine`，别用多参数 `Join-Path`。

### 4.2 Pillow `save(ICO, sizes=[...])` 只嵌首张尺寸
**现象**：生成的 `.ico` 实际只含 16×16 一张（几百字节），在资源管理器/任务栏缩放时变糊。
**根因**：Pillow 的 ICO 写入对多尺寸支持有 bug，会丢弃其余尺寸。
**解决**：在 `tools/make_icons.py` 里**手写 PNG-in-ICO 容器**——每个目标尺寸独立 `resize` + 锐化后编码为完整 PNG，再按 ICONDIR/ICONENTRY 结构拼装。
实测嵌入 16/24/32/48/64/128/256 共 7 档全部有效（注意：PIL 打开手写的 ICO 时 `sizes` 字段为空，但用结构体解析每个 PNG 均正常加载，属 PIL 特性而非缺陷）。
**注**：macOS 的 `.icns` 用 Pillow 直接 `save` 即可，无此问题。

### 4.3 `.bat` 编码与换行符（致命坑）
**规则**：`.bat` 必须 **纯 ASCII（或 GBK）+ CRLF 换行 + 无 BOM**。
- 用 Write 工具直接写含中文的 bat 会出现非标准编码（既不是 UTF-8 也不是 GBK），cmd 完全乱码；
- 若写成 LF-only，cmd 解析错位，整段被当成一行命令导致闪退。
**本项目做法**：`build.bat` 全程不写中文（只做 `powershell -NoProfile -ExecutionPolicy Bypass -File build.ps1` 启动），且确保 CRLF + 无 BOM。
**校验脚本**（Python）：`BOM = data[:3]==b'\xef\xbb\xbf'`、`CRLF = data.count(b'\r\n')`、`LFonly = 非 CRLF 的换行数`。正确状态：BOM=False、首字节 `@`、CRLF>0、LFonly=0。

### 4.4 PowerShell 脚本需要 UTF-8 BOM
**现象**：`build.ps1` 若存成无 BOM 的 UTF-8，PowerShell 5.1 按系统默认 GBK 读取，中文 `echo` 全部乱码。
**解决**：给 `build.ps1` 加 UTF-8 BOM（`EF BB BF`）。注意与 4.3 的 bat 相反——bat 不要 BOM，ps1 要 BOM。

### 4.5 三处图标一致性（exe / 窗体左上角 / 任务栏）
- **exe 文件图标**（资源管理器里看到的）：由 `PyInstaller --icon 图标\MMY-AI-Studio-icon.ico` 嵌入；
- **窗体左上角图标**：`main.py` 用打包进去的 `MMY-AI-Studio-icon.png` 调 `app.setWindowIcon(QIcon(...))`；
- **任务栏图标**：Windows 对 Qt 程序默认直接用 exe 内嵌图标，与上述同源，天然一致。
**兜底**：`main.py` 增加判断——若打包后 PNG 资源缺失，自动回退到 exe 自身内嵌图标
（`QFileIconProvider().icon(QFileInfo(sys.executable))`），保证左上角与任务栏永远有图标且不脱节。

### 4.6 Windows 图标路径缓存（重打包不见新图标）
**现象**：重新构建 exe 后，文件管理器里**原路径同名 exe 仍显示旧图标**，复制到别处就正常。
**根因**：Windows 资源管理器按**文件路径**缓存 exe 图标（`iconcache*.db`）。
**解决**：打包脚本先删除旧的 `dist\iconCut.exe` 再构建；README 提示用户遇到时刷新 `dist\` 目录或重启资源管理器。
**更稳的做法**：构建产物输出到带版本号/时间戳的子目录，避免原地覆盖同名文件。

### 4.7 PyInstaller 单文件关键参数
```
--onefile           单文件（便携）
--windowed          无控制台窗口（GUI 程序）
--name iconCut      产物名
--icon <ico>        exe 文件图标
--add-data "图标;图标"   把图标目录一起打进 exe（Windows 用 ; 分隔，macOS/Linux 用 :）
```
运行时在代码里用 `sys._MEIPASS` 定位临时解压目录下的 `图标/`，统一走 `resource_path()` 辅助函数。

---

## 5. 构建产物与发布

- 生成物（`dist/`、`build/`、`*.spec`、`图标/*.ico`、`图标/*.icns`、`__pycache__/`、`*.pyc`）均已加入 `.gitignore`，不入库，由打包脚本按需重新生成。
- 发布 release 时，把 `dist\iconCut.exe`（Windows）与 `dist/iconCut.app`（macOS）作为附件上传即可。

---

## 6. 常见问题速查

| 问题 | 原因 | 处理 |
|------|------|------|
| `Join-Path` 报位置参数错误 | PS5.1 多段路径 | 改用 `Path.Combine` |
| exe 图标在高分屏发虚 | ICO 缺大尺寸 | 手写多尺寸 ICO |
| bat 双击乱码/闪退 | 编码或 LF 换行 | 转 GBK+CRLF+无 BOM |
| ps1 中文乱码 | 缺 UTF-8 BOM | 加 BOM |
| 重打包图标没变 | 路径缓存 | 删旧 exe / 重启资源管理器 |
| 任务栏图标不对 | 未设 `--icon` | 用 `--icon` 嵌入，与窗体同源 |
